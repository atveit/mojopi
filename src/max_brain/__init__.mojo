# max_brain: Mojo-side wrappers around Python MAX pipelines.
#
# MAX is Python-first (2026). This package bridges the Mojo agent runtime
# to `max.pipelines` + `max.kv_cache` via `from python import Python`.
